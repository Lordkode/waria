name: Backend Deploy

on:
  workflow_call:
    inputs:
      trigger_source:
        required: true
        type: string

jobs:
  deploy:
    if: >
      ${{ github.event.inputs.trigger_source == 'Backend E2E Tests' &&
          github.event.workflow_run.conclusion == 'success' &&
          github.ref == 'refs/heads/prod' }}
    runs-on: ubuntu-latest
    defaults: 
      run: 
        working-directory: ./backend
        
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Deploy to server
        run: |
          echo "🚀 Deploying backend to production server..."
          # ton script de déploiement ici (rsync, ssh, ftp, etc.)
